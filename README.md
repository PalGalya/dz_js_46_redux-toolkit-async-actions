# 🏠 Домашнє завдання №28

## Рефакторинг існуючого Redux Toolkit проекту з використанням асинхронних дій

> **Мета завдання:** Додати асинхронні дії до існуючого Redux Toolkit проекту, використовуючи Redux Thunk або Redux Saga для управління асинхронними операціями (наприклад, запитами до API).

---

## 📋 Завдання виконано

### ✅ Основні вимоги:

- [x] Проект ініціалізовано за допомогою Vite
- [x] Redux Toolkit встановлено та налаштовано
- [x] Використано `createAsyncThunk` для створення асинхронних дій
- [x] Оновлено існуючі slices, додавши обробку станів `pending`, `fulfilled` та `rejected`
- [x] Підключено асинхронні дії до компонентів через `useDispatch` та `useSelector`

### 🚀 Реалізовані функції:

#### 1. **Асинхронна дія**

- Завантаження користувачів з [JSONPlaceholder API](https://jsonplaceholder.typicode.com/users)
- Використання `createAsyncThunk` замість Redux Thunk

#### 2. **Обробка станів в extraReducers:**

- **`pending`** - встановлює прапор завантаження
- **`fulfilled`** - зберігає отримані дані
- **`rejected`** - обробляє помилки

#### 3. **UI індикація:**

- Показ стану завантаження під час запиту
- Блокування кнопки під час виконання запиту
- Відображення списку завантажених користувачів

---

## 🏗️ Структура проекту

```
dz_js28/
├── src/
│   ├── redux/
│   │   ├── store.js                    # Конфігурація Redux store
│   │   ├── slices/
│   │   │   └── exampleSlice.js         # Slice з extraReducers для async дій
│   │   └── asyncActions/
│   │       └── fetchData.js            # createAsyncThunk дії
│   ├── components/
│   │   └── ExampleComponent.jsx        # Компонент з useDispatch/useSelector
│   ├── App.jsx                         # Головний компонент
│   ├── App.css                         # Стилі
│   └── main.jsx                        # Точка входу
├── package.json                        # Залежності проекту
└── README.md                          # Документація
```

---

## 🛠️ Технології

- **React** 18.2.0 - основний фреймворк
- **Redux Toolkit** 2.0.1 - для управління станом
- **React-Redux** 9.0.4 - для підключення Redux до React
- **Vite** - інструмент збірки та розробки
- **JSONPlaceholder** - тестовий API

---

## 🚀 Встановлення та запуск

1. **Клонування репозиторію:**

```bash
git clone <your-repo-url>
cd dz_js28
```

2. **Встановлення залежностей:**

```bash
npm install
```

3. **Запуск у режимі розробки:**

```bash
npm run dev
```

4. **Збірка для продакшн:**

```bash
npm run build
```

---

## 🎯 Ключові концепції

### `createAsyncThunk`

```javascript
export const fetchUsers = createAsyncThunk("example/fetchUsers", async () => {
  const response = await fetch("https://jsonplaceholder.typicode.com/users");
  const data = await response.json();
  return data;
});
```

### `extraReducers`

```javascript
extraReducers: (builder) => {
  builder
    .addCase(fetchUsers.pending, (state) => {
      state.isLoading = true;
    })
    .addCase(fetchUsers.fulfilled, (state, action) => {
      state.isLoading = false;
      state.users = action.payload;
    })
    .addCase(fetchUsers.rejected, (state) => {
      state.isLoading = false;
    });
};
```

### Використання в компоненті

```javascript
const dispatch = useDispatch();
const { users, isLoading } = useSelector((state) => state.example);

const handleFetchUsers = () => {
  dispatch(fetchUsers());
};
```

---


## 👨‍💻 Автор
Halyna Palamarchuk

Студентка курсу Hillel IT School_ 🎓
